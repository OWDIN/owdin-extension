import React from 'react'
import {
  Layout,
} from 'antd'

const { Footer } = Layout

const FooterWrapper = () => {
  return (
    <Footer
      style={{
        textAlign: 'center',
      }}
    >
      &copy; OWDIN Network
    </Footer>
  )
}

export default FooterWrapper
